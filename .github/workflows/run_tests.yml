name: Run Unit Tests
on: [push]

jobs:
  unit_tests:
    name: python
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Run tests
        run: |
            uv venv --python 3.11
            source .venv/bin/activate
            uv pip install .
            uv run pytest
